/target

# notes/ is its own git repo (nb notebook) — tracked separately, not vendored here
/notes

# lab scratch artifacts
/lab-bounce.wav
/lab-session.ron

# local tool config
/reasonix.toml

# Generated Python bytecode (TAKE / analysis tooling)
__pycache__/
*.pyc
